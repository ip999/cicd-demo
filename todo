fix tests for backend
add frontend (use fresh?)
add ci github actions
check if shiftleft is still a thing?
figure out tags/releases
add cd github actions
add deployment (dockerhub)
add terraform

once all is working, protect main branch

if we get all fancy with appsec...
add sql part to app
https://www.npmjs.com/package/better-sqlite3
